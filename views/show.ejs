<!DOCTYPE html>
<html>
<head>
	<title><%= movie.title %> page</title>
</head>
<body>

	<h1><%= movie.title %></h1>

	<p> <%= movie.yearReleased %></p>

	<p> You have
		<% if(!movie.hasBeenWatched) { %>
			not
		<% } %>
		watched it.
	</p>

	<a href="<%= movie.id %>/edit"><button type="submit">Edit <%= movie.title %></button></a>

	<a href="/movies"><button type="submit">Back to index page</button></a>

</body>
</html>