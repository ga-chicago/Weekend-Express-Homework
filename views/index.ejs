<!doctype html>
<html>
<head>
    <title>Unicorn Index Page</title>
    <link type="text/css" rel="stylesheet" href="/public/css/style.css" >
</head>
<body>
<h1>Unicorn Index Page</h1>
<a href="/unicorns/new">🌈 🌟 Create a new unicorn breed 🦄 🍬</a>

<ul>
    <% for(let i=0; i < unicorns.length; i++) { %>

    <li>
        The Unicorn's name is
        <a href="/unicorns/<%= i %>"></a>
        <%= unicorns[i].name %>

        and is
        <%= unicorns[i].color %>
        in color. <%= unicorns[i].name %> is
        <% if(!unicorns[i].hungry) { %> not <% } %>
        <% if(unicorns[i].hungry) { %> 🍦very🍭 <% } %>
        hungry.

        <form action="/unicorns/<%= unicorns[i].id %>?_method=DELETE" method="POST">
            <input type="submit" value="Delete">
        </form>
    </li>

    <a href="/unicorns/<%= unicorns[i].id %>/edit">Edit</a>

    <% } %>
</ul>

</body>
</html>